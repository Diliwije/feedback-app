export default function FeedbackList(
    { initialPosts, 
        userId }: 

    {   initialPosts: any[], 
        userId: string | null }) 
    {
        return (
            <div>
                <h2>FeedBack List</h2>
            </div>
  );
}